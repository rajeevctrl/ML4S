package com.ml4s.np.temp


import com.ml4s.np.temp.{NP => np}
import com.ml4s.np.temp.NPImplicits._

//import org.nd4s.Implicits._

object Main {
  def main(args:Array[String]):Unit={
    
  }
  
  //case object Employee
  
  
}